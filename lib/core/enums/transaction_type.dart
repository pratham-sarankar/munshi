/// Represents the type of a financial transaction.
enum TransactionType {
  /// A transaction that adds money (e.g., salary, freelance).
  income,

  /// A transaction that removes money (e.g., shopping, food).
  expense,
}
