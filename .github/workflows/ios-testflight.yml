name: 🚀 iOS TestFlight Upload

on:
  push:
    branches:
      - main

jobs:
  build-and-upload:
    runs-on: macos-15  # macOS Sequoia with Xcode 16

    env:
      FLUTTER_VERSION: '3.35.5'
      APPSTORE_KEY_ID: ${{ secrets.APPSTORE_KEY_ID }}
      APPSTORE_ISSUER_ID: ${{ secrets.APPSTORE_ISSUER_ID }}
      APPSTORE_KEY: ${{ secrets.APPSTORE_KEY }}
      MATCH_GIT_BASIC_AUTHORIZATION: ${{ secrets.MATCH_GIT_BASIC_AUTHORIZATION }}
      MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}

    steps:
      - name: 🧹 Checkout repository
        uses: actions/checkout@v4

      - name: 🧰 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable

      - name: 🔑 Decode App Store Connect API key
        run: |
          mkdir -p ios
          echo "$APPSTORE_KEY" | base64 --decode > ios/AuthKey_${APPSTORE_KEY_ID}.p8

      - name: 💎 Install Fastlane & CocoaPods globally
        run: |
          sudo gem install fastlane -v 2.228.0
          sudo gem install cocoapods -v 1.16.2
          pod repo update

      - name: 🚀 Build & Upload via Fastlane
        working-directory: ios
        run: |
          # print pod and ruby versions
          pod --version
          ruby -v
          fastlane beta
