# Categories Feature Architecture

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                         UI Layer                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │         Categories Screen (Stateful Widget)          │  │
│  │  ┌────────────┐         ┌────────────┐              │  │
│  │  │  Expense   │         │   Income   │              │  │
│  │  │    Tab     │         │    Tab     │              │  │
│  │  └────────────┘         └────────────┘              │  │
│  │                                                      │  │
│  │  ┌────────────────────────────────────────────┐     │  │
│  │  │         Category List                      │     │  │
│  │  │  ┌──────────────────────────────────┐     │     │  │
│  │  │  │  CategoryListTile (Widget)       │     │     │  │
│  │  │  │  - Icon Container                │     │     │  │
│  │  │  │  - Category Name                 │     │     │  │
│  │  │  │  - Default Badge (if default)    │     │     │  │
│  │  │  │  - Edit/Delete Buttons           │     │     │  │
│  │  │  └──────────────────────────────────┘     │     │  │
│  │  └────────────────────────────────────────────┘     │  │
│  │                                                      │  │
│  │  [+ Add Category] ← FAB                             │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │    AddEditCategoryDialog (Stateful Widget)           │  │
│  │  ┌────────────────────────────────────────────┐     │  │
│  │  │  Category Name TextField                   │     │  │
│  │  └────────────────────────────────────────────┘     │  │
│  │  ┌────────────────────────────────────────────┐     │  │
│  │  │  Icon Selector ──→ IconPickerDialog        │     │  │
│  │  └────────────────────────────────────────────┘     │  │
│  │  ┌────────────────────────────────────────────┐     │  │
│  │  │  Color Selector ──→ ColorPickerDialog      │     │  │
│  │  └────────────────────────────────────────────┘     │  │
│  │  [Cancel] [Save/Update]                             │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    State Management Layer                   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │         CategoryProvider (ChangeNotifier)            │  │
│  │                                                      │  │
│  │  State:                                              │  │
│  │  - List<Category> expenseCategories                 │  │
│  │  - List<Category> incomeCategories                  │  │
│  │  - bool isLoading                                   │  │
│  │                                                      │  │
│  │  Methods:                                            │  │
│  │  - loadCategories()                                 │  │
│  │  - addCategory(category)                            │  │
│  │  - updateCategory(category)                         │  │
│  │  - deleteCategory(id)                               │  │
│  │  - categoryNameExists(name, type)                   │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     Data Access Layer                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │         CategoriesDao (Database Accessor)            │  │
│  │                                                      │  │
│  │  Methods:                                            │  │
│  │  - getAllCategories()                               │  │
│  │  - getCategoriesByType(type)                        │  │
│  │  - getExpenseCategories()                           │  │
│  │  - getIncomeCategories()                            │  │
│  │  - getCategoryById(id)                              │  │
│  │  - insertCategory(category)                         │  │
│  │  - updateCategory(category)                         │  │
│  │  - deleteCategory(id)                               │  │
│  │  - categoryNameExists(name, type, excludeId)        │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      Database Layer                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              AppDatabase (Drift)                     │  │
│  │                                                      │  │
│  │  Schema Version: 2                                   │  │
│  │                                                      │  │
│  │  Tables:                                             │  │
│  │  - Transactions (existing)                          │  │
│  │  - Categories (new)                                 │  │
│  │                                                      │  │
│  │  Migration Strategy:                                 │  │
│  │  - onCreate: Create all tables + seed data          │  │
│  │  - onUpgrade: Add categories table + seed data      │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │           Categories Table Schema                    │  │
│  │  ┌────────────────────────────────────────────┐     │  │
│  │  │ id               INTEGER PRIMARY KEY       │     │  │
│  │  │ name             TEXT NOT NULL             │     │  │
│  │  │ iconCodePoint    INTEGER NOT NULL          │     │  │
│  │  │ iconFontFamily   TEXT NULLABLE             │     │  │
│  │  │ iconFontPackage  TEXT NULLABLE             │     │  │
│  │  │ colorValue       INTEGER NOT NULL          │     │  │
│  │  │ type             TEXT NOT NULL             │     │  │
│  │  │ isDefault        BOOLEAN DEFAULT FALSE     │     │  │
│  │  │ createdAt        DATETIME AUTO             │     │  │
│  │  └────────────────────────────────────────────┘     │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 📊 Data Flow

### Read Operation (Loading Categories)
```
User opens Categories screen
         ▼
CategoriesScreen calls Provider
         ▼
CategoryProvider.loadCategories()
         ▼
CategoriesDao.getExpenseCategories()
CategoriesDao.getIncomeCategories()
         ▼
Database queries executed
         ▼
Results returned as List<Category>
         ▼
Provider updates state & notifies listeners
         ▼
UI rebuilds with new data
```

### Create Operation (Adding Category)
```
User taps "Add Category" FAB
         ▼
AddEditCategoryDialog opens
         ▼
User fills form (name, icon, color)
         ▼
User taps "Add" button
         ▼
Validation runs (required, duplicates)
         ▼
Provider.addCategory(category)
         ▼
Dao.insertCategory(category)
         ▼
Database INSERT executed
         ▼
Provider.loadCategories() auto-refresh
         ▼
UI updates with new category
         ▼
Success message shown
```

### Update Operation (Editing Category)
```
User taps Edit icon on category
         ▼
AddEditCategoryDialog opens with data
         ▼
User modifies fields
         ▼
User taps "Update" button
         ▼
Validation runs
         ▼
Provider.updateCategory(category)
         ▼
Dao.updateCategory(category)
         ▼
Database UPDATE executed
         ▼
Provider.loadCategories() auto-refresh
         ▼
UI updates with changes
         ▼
Success message shown
```

### Delete Operation (Removing Category)
```
User taps Delete icon on category
         ▼
Confirmation dialog appears
         ▼
User confirms deletion
         ▼
Provider.deleteCategory(id)
         ▼
Dao.deleteCategory(id)
         ▼
Database DELETE executed
         ▼
Provider.loadCategories() auto-refresh
         ▼
UI updates (category removed)
         ▼
Success message shown
```

## 🔄 State Management Flow

```
┌──────────────┐
│    Widget    │
│   (Screen)   │
└──────┬───────┘
       │ 1. Consumer<CategoryProvider>
       │    watches for changes
       ▼
┌──────────────┐
│  Category    │
│   Provider   │
└──────┬───────┘
       │ 2. Calls DAO methods
       │    when actions occur
       ▼
┌──────────────┐
│  Categories  │
│     DAO      │
└──────┬───────┘
       │ 3. Executes database
       │    queries via Drift
       ▼
┌──────────────┐
│  Database    │
│   (SQLite)   │
└──────────────┘
       │
       │ 4. Results returned
       │    up the chain
       ▼
┌──────────────┐
│  Provider    │
│ notifyListeners()
└──────┬───────┘
       │ 5. UI rebuilds
       │    automatically
       ▼
┌──────────────┐
│    Widget    │
│   Updates    │
└──────────────┘
```

## 🎯 Dependency Graph

```
main.dart
  └─→ CategoryProvider ────┐
         ▲                  │
         │                  │
CategoriesScreen            │
  ├─→ CategoryListTile      │
  ├─→ AddEditCategoryDialog │
  │    ├─→ IconPickerDialog │
  │    └─→ ColorPickerDialog│
  │                          │
  └──────────────────────────┘
                             │
                             ▼
                    CategoriesDao
                             │
                             ▼
                      AppDatabase
                       (Drift)
                             │
                             ▼
                     Categories Table
                        (SQLite)
```

## 📦 Module Dependencies

```
Features Layer:
├── categories/
│   ├── screens/ ─────→ providers/
│   ├── widgets/ ─────→ providers/
│   └── providers/ ───→ core/database/daos/

Core Layer:
└── database/
    ├── daos/ ────────→ app_database.dart
    ├── tables/ ──────→ app_database.dart
    └── app_database.dart (generated)

External:
├── Provider (state management)
├── Drift (database ORM)
├── Icons Plus (icon library)
└── Flutter (UI framework)
```

## 🛠️ Technology Stack

```
┌─────────────────────────┐
│    Presentation Layer   │
│    ┌─────────────────┐  │
│    │     Flutter     │  │ ← UI Framework
│    └─────────────────┘  │
│    ┌─────────────────┐  │
│    │  Material       │  │ ← Design System
│    │  Design 3       │  │
│    └─────────────────┘  │
│    ┌─────────────────┐  │
│    │   Icons Plus    │  │ ← Icon Library
│    │   (Iconsax)     │  │
│    └─────────────────┘  │
└─────────────────────────┘
           │
┌─────────────────────────┐
│    Business Logic Layer │
│    ┌─────────────────┐  │
│    │    Provider     │  │ ← State Management
│    │ (ChangeNotifier)│  │
│    └─────────────────┘  │
└─────────────────────────┘
           │
┌─────────────────────────┐
│    Data Access Layer    │
│    ┌─────────────────┐  │
│    │   Drift ORM     │  │ ← Database ORM
│    │  (Type-safe)    │  │
│    └─────────────────┘  │
└─────────────────────────┘
           │
┌─────────────────────────┐
│   Persistence Layer     │
│    ┌─────────────────┐  │
│    │     SQLite      │  │ ← Database Engine
│    └─────────────────┘  │
└─────────────────────────┘
```

## 🔐 Data Model

```
Category Entity
┌──────────────────────┐
│ id: int (PK)         │ ← Auto-increment
│ name: String         │ ← Category name
│ iconCodePoint: int   │ ← Icon code
│ iconFontFamily: str? │ ← Font family
│ iconFontPackage: str?│ ← Package name
│ colorValue: int      │ ← Color as int
│ type: String         │ ← 'expense'/'income'
│ isDefault: bool      │ ← Default flag
│ createdAt: DateTime  │ ← Timestamp
└──────────────────────┘
         │
         ├─→ Used in Transactions
         └─→ Displayed in Categories Screen
```

## 🎨 Widget Tree

```
CategoriesScreen (StatefulWidget)
├── AppBar
│   ├── Title: "Categories"
│   └── TabBar
│       ├── Tab: "Expense"
│       └── Tab: "Income"
├── TabBarView
│   ├── _CategoryList (expense)
│   │   └── Consumer<CategoryProvider>
│   │       ├── CircularProgressIndicator (if loading)
│   │       ├── Empty State Widget (if empty)
│   │       └── ListView.builder
│   │           └── CategoryListTile
│   │               ├── Icon Container
│   │               ├── Category Name
│   │               ├── Default Badge (conditional)
│   │               └── Action Buttons
│   │                   ├── Edit Button
│   │                   └── Delete Button
│   └── _CategoryList (income)
│       └── (same structure)
└── FloatingActionButton
    └── "Add Category"
```

---

**Architecture designed for:**
- ✅ Maintainability
- ✅ Testability
- ✅ Scalability
- ✅ Performance
- ✅ Type safety
